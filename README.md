### Sign Language Detection Page

This project creates a website which features a webcam view that allows the user to gesture American sign language (ASL) letters. 
The webcam uses a neural network in the backend to identify the user's gesture. This is a page where users can test out their skills
in ASL to make sure they are correctly performing gestures in ASL.


